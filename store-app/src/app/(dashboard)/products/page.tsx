"use client";

import { Container, Grid } from "@mui/material";

export default function ProductView() {
  const titles = ["Nome", "Preço", "Descrição"];

  return (
    <Container>
      <Grid item xs={12} md={100} lg={100}>
      </Grid>
    </Container>
  );
}
