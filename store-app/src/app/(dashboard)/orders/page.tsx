'use client';

import { Container, Grid } from "@mui/material";

export default function OrdersView() {
  const titles = ["ID Cliente", "ID Produto", "Qntd Produto", "Valor Total", "Status do Pedido"];

  return (
    <Container>
      <Grid item xs={12} md={100} lg={100}>
      </Grid>
    </Container>
  );
}
