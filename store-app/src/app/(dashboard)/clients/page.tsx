"use client";

import { Container, Grid } from "@mui/material";

export default function ClientView() {
  const titles = ["Nome", "Email", "Telefone", "CPF"];

  return (
    <Container>
      <Grid item xs={12} md={100} lg={100}>

      </Grid>
    </Container>
  );
}
